Build the LettiB project foundation. LettiB is a multi-AI workspace SaaS app where users bring their own API keys, compare responses from multiple AI models, and generate a merged "LettiB Synthesis" answer saved into project folders.

Do not write any feature code yet. Foundation only.

STACK:
- Next.js 14 App Router, TypeScript, Tailwind CSS, ESLint
- shadcn/ui for components
- Supabase for auth, database, storage
- Vercel AI SDK for AI provider calls
- Deployment target: Vercel

STEP 1 — Initialize project:
- Next.js 14 with App Router, TypeScript, Tailwind CSS, ESLint
- Do NOT use the Pages Router under any circumstances

STEP 2 — Install dependencies:
npm install @supabase/supabase-js @supabase/ssr ai @ai-sdk/openai @ai-sdk/anthropic @ai-sdk/google zod lucide-react

STEP 3 — Install and initialize shadcn/ui:
- Base color: neutral
- CSS variables: yes
- Add these components: button card dialog dropdown-menu input label select tabs textarea toast command sheet badge separator

STEP 4 — Create this exact folder structure:
/app
  /(auth)
    /login/page.tsx
    /signup/page.tsx
  /(app)
    /dashboard/page.tsx
    /projects/page.tsx
    /projects/[id]/page.tsx
    /chat/page.tsx
    /compare/page.tsx
    /synthesis/[id]/page.tsx
    /teams/page.tsx
    /settings/page.tsx
    /usage/page.tsx
    /admin/page.tsx
  /(marketing)
    /page.tsx
    /pricing/page.tsx
    /privacy/page.tsx
    /terms/page.tsx
    /roadmap/page.tsx
  /api
    /chat/route.ts
    /compare/route.ts
    /synthesis/route.ts
    /keys/route.ts
/lib
  /supabase
    client.ts
    server.ts
    middleware.ts
  /providers
    index.ts
    openai.ts
    anthropic.ts
    google.ts
    xai.ts
    models.ts
  /prompts
    synthesis.ts
    memory.ts
  /utils.ts
/components
  /ui (shadcn components go here automatically)
  /layout
    sidebar.tsx
    bottom-nav.tsx
    header.tsx
  /projects
    project-card.tsx
  /chat
    message-bubble.tsx
  /compare
    response-card.tsx
  /usage
    usage-widget.tsx
/tests
  placeholder.spec.ts
/supabase
  /migrations
    .gitkeep

STEP 5 — Create .env.example with these exact variables and no real values:
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
VAULT_ENCRYPTION_KEY=
ALLOWED_ADMIN_EMAILS=
ANTHROPIC_API_KEY=
RESEND_API_KEY=
STANDUP_EMAIL_TO=

STEP 6 — Create .env.local with the same keys, all empty. Add .env.local to .gitignore. Confirm .gitignore already includes: .env.local, .env, node_modules, .next

STEP 7 — Create lib/providers/models.ts with this exact content:

export const MODELS_CATALOG = {
  openai: [
    { id: 'gpt-5.5',       name: 'GPT-5.5 (Frontier)', context: 1000000, cost_in: 5.00,  cost_out: 30.00 },
    { id: 'gpt-5.4',       name: 'GPT-5.4',            context: 400000,  cost_in: 2.50,  cost_out: 15.00 },
    { id: 'gpt-5.4-mini',  name: 'GPT-5.4 Mini',       context: 400000,  cost_in: 0.75,  cost_out: 4.50  },
    { id: 'gpt-5.4-nano',  name: 'GPT-5.4 Nano',       context: 400000,  cost_in: 0.20,  cost_out: 1.25  },
    { id: 'gpt-5.1',       name: 'GPT-5.1',            context: 400000,  cost_in: 1.25,  cost_out: 10.00 },
  ],
  anthropic: [
    { id: 'claude-opus-4-7',           name: 'Claude Opus 4.7',  context: 1000000, cost_in: 5.00, cost_out: 25.00 },
    { id: 'claude-sonnet-4-6',         name: 'Claude Sonnet 4.6',context: 1000000, cost_in: 3.00, cost_out: 15.00 },
    { id: 'claude-haiku-4-5-20251001', name: 'Claude Haiku 4.5', context: 200000,  cost_in: 1.00, cost_out: 5.00  },
  ],
  google: [
    { id: 'gemini-3.1-pro',   name: 'Gemini 3.1 Pro',   context: 1000000, cost_in: 2.00, cost_out: 12.00 },
    { id: 'gemini-3-flash',   name: 'Gemini 3 Flash',   context: 1000000, cost_in: 0.50, cost_out: 3.00  },
    { id: 'gemini-2.5-pro',   name: 'Gemini 2.5 Pro',   context: 1000000, cost_in: 1.25, cost_out: 10.00 },
    { id: 'gemini-2.5-flash', name: 'Gemini 2.5 Flash', context: 1000000, cost_in: 0.30, cost_out: 2.50  },
  ],
  xai: [
    { id: 'grok-4.1', name: 'Grok 4.1', context: 256000, cost_in: 0.20, cost_out: 0.50  },
    { id: 'grok-4',   name: 'Grok 4',   context: 256000, cost_in: 3.00, cost_out: 15.00 },
  ],
} as const;

export type Provider = keyof typeof MODELS_CATALOG;
export type ModelEntry = typeof MODELS_CATALOG[Provider][number];

export const DEFAULT_TEAM_MODELS = {
  research:   ['gpt-5.4', 'claude-opus-4-7', 'gemini-3.1-pro'],
  coding:     ['claude-sonnet-4-6', 'gpt-5.4'],
  brainstorm: ['claude-opus-4-7', 'gpt-5.4', 'grok-4.1'],
} as const;

STEP 8 — Create lib/prompts/synthesis.ts with this exact content:

export const SYNTHESIS_PROMPT = `You are LettiB Synthesis. Your job is to merge multiple AI responses into one final answer that is better than any single response.

Do NOT simply average. Instead:
1. Identify the strongest, most accurate points from each response
2. Remove redundant information
3. Resolve contradictions explicitly — note when models disagree and which seems more credible
4. Preserve specific details, numbers, and citations
5. Acknowledge uncertainty when responses do not agree
6. Make the final answer clear, organized, and actionable
7. Use the user-selected tone throughout

CONSENSUS HANDLING:
When source responses largely agree on a topic:
- Briefly state the consensus position with confidence
- Then earn your existence by ONE of these:
  (a) Highlight a precise detail one source captured better than others
  (b) Flag the single most common mistake people make on this topic
  (c) Identify what the convergence missed — a real edge case, not a manufactured one
  (d) If consensus is total and there is nothing useful to add, keep the synthesis SHORT and tell the user the original sources are sufficient. Do not pad.

CONTROVERSY HANDLING:
When sources disagree:
- Name the disagreement explicitly with attribution
- Pick a side if the evidence supports it
- Refuse to pick if it does not
- Make the user smarter about WHY this question is contested

CRITICAL: Do not invent information not present in the source responses. Do not claim accuracy beyond what the sources support. Start directly with the answer — no preamble. End with one sentence on the most useful next step for the user.

User question: {{question}}
Project context: {{project_context}}
Tone: {{tone}}
Model responses: {{responses}}`;

export const MEMORY_INJECTION_PROMPT = `Project memory (background context for this conversation):

GOAL: {{project_goal}}
DECISIONS: {{important_decisions}}
PREFERENCES: {{user_preferences}}
KEY FACTS: {{key_facts}}
OPEN QUESTIONS: {{open_questions}}
NEXT STEPS: {{next_steps}}

Use this context to inform your responses. Do not repeat it back unless asked.`;

STEP 9 — Create middleware.ts in the project root:
- Protect these routes: /dashboard, /projects, /chat, /compare, /synthesis, /teams, /settings, /usage, /admin
- Redirect unauthenticated users to /login
- Redirect authenticated users away from /login and /signup to /dashboard
- Use @supabase/ssr createServerClient pattern
- Export config with a matcher that excludes _next/static, _next/image, favicon.ico

STEP 10 — Create each page file as a minimal placeholder that compiles without errors:
- Each page renders its route name as an h1: <h1>Dashboard</h1>, <h1>Projects</h1>, etc.
- Add "use client" directive only to pages that will need it later (compare, chat)
- All other pages are server components by default (no directive needed)
- API routes return a basic JSON response: { status: "ok", route: "/api/chat" }

STEP 11 — Create lib/supabase/client.ts, server.ts, and middleware.ts using the @supabase/ssr pattern. Use environment variables. Do not hardcode any values.

STEP 12 — Create components/layout/sidebar.tsx as a basic nav component with links to: Dashboard, Projects, Compare, Chat, Teams, Settings, Usage. Desktop only (hidden on mobile).

STEP 13 — Create components/layout/bottom-nav.tsx with 4 items: Home, Projects, New, Settings. Mobile only (hidden on desktop).

STEP 14 — Create components/layout/header.tsx with: LettiB logo text on the left, a search icon button on the right (⌘K placeholder, no functionality yet).

STEP 15 — Create .github/workflows/ci.yml that:
- Triggers on push and pull_request to main
- Runs: npm ci, npm run lint, npm run build
- Uses Node.js 20
- Does NOT run Playwright yet (add that in a later phase)

STEP 16 — Create a README.md with:
- Project name: LettiB
- One-line description: Multi-AI workspace for AI power users
- Local setup instructions (clone, npm install, copy .env.example to .env.local, fill in vars, npm run dev)
- Tech stack list
- Link to docs (placeholder: #)

STEP 17 — Verify the build:
- Run npm run build
- Fix any TypeScript or ESLint errors until the build passes cleanly
- Confirm no "use client" / "use server" conflicts
- Confirm middleware.ts compiles without errors

HARD RULES — enforce these throughout:
1. No API keys or secrets in any file other than .env.local
2. .env.local must be in .gitignore
3. No "use client" on pages that do not need it
4. All imports must resolve — no placeholder imports that will break the build
5. TypeScript strict mode on
6. The build must pass npm run build at the end of this prompt with zero errors

When complete, confirm: folder structure created, build passes, middleware compiles, all placeholder pages render their route name.